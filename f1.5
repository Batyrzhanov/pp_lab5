from itertools import permutations

def get_permutations(input_string):
    perms = permutations(input_string)

    for perm in perms:
        print("".join(perm))

user_input = input("Enter a string: ")
print("Permutations of the input string:")
get_permutations(user_input)
